# Server-side only. Do NOT prefix with VITE_ — that would expose it in the browser.
# Used by the backend (e.g. Vercel Function under /api) to call Gemini.
# Frontend should call the backend route, not Gemini directly.
GEMINI_API_KEY="MY_GEMINI_API_KEY"
